<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="5dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="5dp">

    <TextView
        android:id="@+id/item_search_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:textColor="@android:color/black"
        android:textSize="15sp" />

    <com.facebook.drawee.view.SimpleDraweeView
        android:layout_below="@id/item_search_name"
        android:id="@+id/item_search_imageview"
        android:layout_marginTop="10dp"
        android:layout_width="80dp"
        android:layout_height="60dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        fresco:placeholderImage="@drawable/ic_default" />

    <TextView
        android:id="@+id/item_search_remake"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_alignTop="@id/item_search_imageview"
        android:layout_toRightOf="@id/item_search_imageview"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/item_search_click"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:layout_alignBottom="@id/item_search_imageview"
        android:layout_toRightOf="@id/item_search_imageview"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@android:color/darker_gray"
        android:textSize="11sp" />


</RelativeLayout>